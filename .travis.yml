language: dart
dart:
# Install the latest stable release
#- stable
# Install the latest dev release
- dev
before_install:
 - cd $TRAVIS_BUILD_DIR/appbase
dart_task:
   test
#- test: --platform vm
#- test: --platform chrome
# Warnings are fatal, but we only analyze the lib/ directory.
- dartanalyzer: --fatal-warnings lib
